import React from 'react';

import '../../../styles/components/loadings/Spinner.css';

const Spinner: React.FC = () => {
    return (
        <div className="spinner-component_container"></div>
    );
};

export default Spinner;
